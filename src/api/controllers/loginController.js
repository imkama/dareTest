'use strict';

module.exports = {
    /**
     * Function to do login, need an email + password or session without logged mailbox
     * @param {Object} res response variable
     * @param {Object} req request variable
     */
    login: (req, res) => {
        console.log('on login');
    }
}
